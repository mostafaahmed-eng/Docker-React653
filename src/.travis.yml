sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t mostafahmed-eng/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mostafaahmed-eng/docker-react npm run test