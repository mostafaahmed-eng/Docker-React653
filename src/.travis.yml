sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t mostafaanwar/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mostafaanwar/docker-react npm run test